
package: de.hshannover.f4.trust.irongenlog.publisher.strategies 

class: DhcpStrategy

	publishLogData:

		update:
			identifier1 = mac("MAC")			
			identifier2 = device("DHCPSERVERNAME")
			meta = discoby()
			lifetime = session
									
		notify:
				
		notify:

	method: blub
		notify:
		notify:

	method: blub1
		notify:

